services:
  postgres:
    image: postgres:17
    container_name: crowdfunding_pg
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata:
